import React from 'react';
import CustomeServices from '../components/CustomeServices';

export default function ClientAddServices({ navigation }) {
  return (
    <CustomeServices
      navigation={navigation}
      title="Add Services"
    />
  );
}
